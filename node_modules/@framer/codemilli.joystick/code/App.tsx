import { Override } from "framer"
import { JoyStickData } from "./JoyStickWrapper"

export const onDrag: Override = () => {
    return {
        left: JoyStickData.x,
        top: JoyStickData.y,
    }
}
